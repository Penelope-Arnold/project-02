<head>
  <script
    type="text/javascript"
    src="https://code.jquery.com/jquery-2.1.1.min.js"
  ></script>

  <script src="https://cdnjs.cloudflare.com/ajax/libs/materialize/0.100.2/js/materialize.min.js"></script>

  <link
    rel="stylesheet"
    href="https://cdnjs.cloudflare.com/ajax/libs/materialize/0.100.2/css/materialize.min.css"
  />

  <link
    href="https://fonts.googleapis.com/icon?family=Material+Icons"
    rel="stylesheet"
  />

  <link rel="stylesheet" href="/views//styles/styles.css" type="text/ejs" />

  <!-- <script type="text/javascript" src="/views/js/allTrips.js"></script> -->
</head>

<body>
  <!-- <div class="navbar-fixed"> -->
  <nav>
    <div class="nav-wrapper">
      <a href="#" class="brand-logo left">Home</a>
      <ul id="nav-mobile" class="right">
        <li><a href="#">Home</a></li>
        <li><a href="#">My Profile</a></li>
        <li><a href="#">Logout</a></li>
      </ul>
    </div>
  </nav>
  <!-- </div> -->

  <h3>Most Popular!</h3>
  <div class="mostPopular"></div>

  <ul class="collapsible">
    <li>
      <div class="collapsible-header">
        <i class="material-icons">filter_drama</i>Asia!
      </div>
      <div class="collapsible-body"><span class="asia"></span></div>
    </li>
    <li>
      <div class="collapsible-header">
        <i class="material-icons">place</i>Europe!
      </div>
      <div class="collapsible-body"><span class="europe"></span></div>
    </li>
    <li>
      <div class="collapsible-header">
        <i class="material-icons">whatshot</i>Australia!
      </div>
      <div class="collapsible-body"><span class="australia"></span></div>
    </li>
  </ul>

  <script>
    $(document).on("load", function() {
      $.get("/api/posts", function(data) {
        // log the data to our console
        console.log(data);
      });
    });

    var image;

    for (var i = 1; i < 5; i++) {
      image = $("<img>");
      image.attr("src", "http://lorempixel.com/250/250/abstract/" + i);
      image.attr("class", "tripImage");
      image.attr("id", i);
      image.attr("style", "padding: 10px");
      $(".mostPopular").append(image);

      function test() {
        $.get("/api/posts", function(data) {
          // log the data to our console
          console.log(data);
        });
      }
    }

    for (var i = 1; i < 8; i++) {
      image = $("<img>");
      image.attr("src", "http://lorempixel.com/250/250/city/" + i);
      image.attr("class", "tripImage");
      image.attr("id", i);
      image.attr("style", "padding: 10px");
      $(".asia").append(image);
    }

    for (var i = 1; i < 8; i++) {
      image = $("<img>");
      image.attr("src", "http://lorempixel.com/250/250/" + i);
      image.attr("class", "tripImage");
      image.attr("id", i);
      image.attr("style", "padding: 10px");
      $(".europe").append(image);
    }

    for (var i = 1; i < 8; i++) {
      image = $("<img>");
      image.attr("src", "http://lorempixel.com/250/250/sports/" + i);
      image.attr("class", "tripImage");
      image.attr("id", i);
      image.attr("style", "padding: 10px");
      $(".australia").append(image);
    }
  </script>
</body>
